{
  "name": "@family-keeper/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "poetry run pytest --cov=app --cov-report=term-missing",
    "lint": "poetry run ruff check app tests",
    "lint:fix": "poetry run ruff check --fix app tests"
  }
}
